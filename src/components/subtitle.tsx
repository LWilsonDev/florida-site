import React from "react"
import Hr from "./hr"

const Subtitle = ({ text }) => {
  return (
    <div className=" mt-4 mb-4">
      <h2 className="subtitle">{text}</h2>
    </div>
  )
}

export default Subtitle
