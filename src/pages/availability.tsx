import React from "react"
import Layout from "../components/layout"

const Availability = ({ data, location }) => {
  return (
    <Layout title={"Availability"} location={location}>
      <h1>I’m in a layout!</h1>
    </Layout>
  )
}

export default Availability
