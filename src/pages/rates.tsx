import React from "react"
import Layout from "../components/layout"

const Rates = ({ data, location }) => {
  return (
    <Layout title={"Rates"} location={location}>
      <h1>I’m in a layout!</h1>
    </Layout>
  )
}

export default Rates
